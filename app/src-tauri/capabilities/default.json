{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Ghost Pointer windows",
  "windows": ["main", "overlay", "aim"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
